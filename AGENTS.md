# Repository Status

- This checkout is currently empty apart from `.git/` and this file.
- There are no manifests, lockfiles, CI workflows, or source files yet, so do not assume a language, package manager, build tool, test runner, or project layout.
- Re-check the repository root before acting; once any scaffold appears, it becomes the real source of truth.
